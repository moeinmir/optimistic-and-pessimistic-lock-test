spring.application.name=account-management

spring.h2.console.enabled=true
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=${DATABASE_PASSWORD}
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

jwt.secret-key:=${JWT_SECRET_KEY}
jwt.access-token-expiration-time: 10
jwt.refresh-token-expiration-time: 15


spring.aop.auto=true
## we could pass logging properties here too but in xml file we could handle more details
#logging.level.root = info
#logging.file.name=log/app.log
#logging.file.path=log
#logging.pattern.console="%d{yyyy-MM-dd HH:mm:ss}[%mdc{requestId}] [%thread] %-5level %logger{36} - %msg%n"
#logging.pattern.file="%d{yyyy-MM-dd HH:mm:ss}[%mdc{requestId}] [%thread] %-5level %logger{36} - %msg%n"